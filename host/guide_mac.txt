Local TCP Bridge - MacOS Setup (Smart Terminal)
-----------------------------------------

To avoid security blocks and environment issues, do this:

1. Open 'Terminal' (Cmd + Space, then type 'Terminal').
2. Type exactly: bash 
3. DRAG the file 'install_setup_mac.sh' into the terminal window and press ENTER.

🔍 SMART SETUP:
- **Auto-Detection**: Finds your Node.js path automatically.
- **Auto-Install**: If Node is missing, it will attempt to install it via Homebrew.
- **Chrome Stability**: Patches the bridge to ensure it runs correctly on MacOS.

👉 IMPORTANT: Restart Chrome fully after installation.

-----------------------------------------
🛡️ SECURITY & IDENTITY
-----------------------------------------
This bridge is locked to the unique Extension ID:
ngbakchodnmhndnghhejmocfadjfekkf

If your Extension ID is different, the bridge will be blocked for safety.
-----------------------------------------
